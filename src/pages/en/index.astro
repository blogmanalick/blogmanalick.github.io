---
import { getCollection } from 'astro:content';
import BlogIndex from '../../layouts/BlogIndex.astro';
import { LANG_EN } from '../../lib/lang';

const posts = await getCollection('blog', ({ id }) => id.startsWith(`${LANG_EN}/`));
---

<BlogIndex lang={LANG_EN} posts={posts} />

