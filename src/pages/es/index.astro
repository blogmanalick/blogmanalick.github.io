---
import { getCollection } from 'astro:content';
import BlogIndex from '../../layouts/BlogIndex.astro';
import { LANG_ES } from '../../lib/lang';

const posts = await getCollection('blog', ({ id }) => id.startsWith(`${LANG_ES}/`));
---

<BlogIndex lang={LANG_ES} posts={posts} />

